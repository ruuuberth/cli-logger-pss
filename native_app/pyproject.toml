[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pss-logger-native"
version = "0.2.0"
description = "CLI application for PixelStarships Logger with XLSX reporting"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "mitmproxy==11.0.2",
  "sqlalchemy==2.0.38",
  "pydantic==2.10.6",
  "pydantic-settings==2.7.1",
  "python-dotenv==1.0.0",
  "psutil>=5.9.0",
  "rich>=13.0.0",
  "openpyxl>=3.0.0",
]

[project.scripts]
pss-native = "app.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[tool.pytest.ini_options]
pythonpath = ["."]
